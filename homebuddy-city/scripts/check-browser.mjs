import {chromium} from '/Users/hyperbox/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
import fs from 'node:fs/promises';
const browser=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true,args:['--use-angle=swiftshader','--enable-webgl']});
const page=await browser.newPage({viewport:{width:1440,height:1000},reducedMotion:'reduce'});const errors=[];page.on('pageerror',e=>errors.push(e.message));
await page.goto('http://localhost:5174/',{waitUntil:'networkidle'});await page.locator('.map-loading').waitFor({state:'hidden'});await page.screenshot({animations:'disabled',path:'/Users/hyperbox/homebuddy/.firecrawl/homebuddy-desktop.png'});
console.log('Desktop',{title:await page.title(),pins:await page.locator('.home-pin').count(),errors});
const canvas=await page.locator('canvas').evaluate(c=>{const gl=c.getContext('webgl2');const p=new Uint8Array(4);gl.readPixels(c.width/2,c.height/2,1,1,gl.RGBA,gl.UNSIGNED_BYTE,p);return{width:c.width,height:c.height,pixel:[...p]};});console.log('Canvas',canvas);
await page.locator('.home-pin').first().hover();await page.locator('.hover-preview').waitFor();console.log('Hover',await page.locator('.preview-details h2').innerText());await page.screenshot({animations:'disabled',path:'/Users/hyperbox/homebuddy/.firecrawl/homebuddy-hover.png'});
await page.locator('.hover-preview').hover();await page.getByRole('button',{name:'View home',exact:true}).click();await page.getByRole('dialog').waitFor();console.log('Selected',await page.locator('#viewer-title').innerText());await page.screenshot({animations:'disabled',path:'/Users/hyperbox/homebuddy/.firecrawl/homebuddy-viewer.png'});
await page.getByRole('button',{name:'Close floor plan',exact:true}).click();await page.getByRole('button',{name:'Studios',exact:true}).click();await page.waitForFunction(()=>document.querySelectorAll('.home-pin').length===2);console.log('Studios',await page.locator('.home-pin').count());
await page.setViewportSize({width:390,height:844});await page.getByRole('button',{name:'All homes',exact:true}).click();await page.waitForFunction(()=>document.querySelectorAll('.home-pin').length===20);await page.waitForTimeout(1500);await page.screenshot({animations:'disabled',path:'/Users/hyperbox/homebuddy/.firecrawl/homebuddy-mobile.png'});console.log('Mobile overflow',await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth));
await page.getByRole('button',{name:'Browse homes'}).click();await page.locator('.list-home').first().click();console.log('Mobile image',await page.locator('.plan-scroll img').evaluate(i=>({loaded:i.complete&&i.naturalWidth>0,width:i.naturalWidth})));await page.screenshot({animations:'disabled',path:'/Users/hyperbox/homebuddy/.firecrawl/homebuddy-mobile-viewer.png'});
await fs.writeFile('/Users/hyperbox/homebuddy/.firecrawl/homebuddy-check.json',JSON.stringify({errors,canvas}));await browser.close();

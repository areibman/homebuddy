# Homebuddy compatibility launcher

The map, floor-plan viewer, decorator, and furniture library now live in `../furniture-catalog`. This folder only forwards old npm commands to that one application. The original standalone project was preserved at `/Users/hyperbox/homebuddy-city-retired-20260908`.

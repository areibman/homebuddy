import { AccountPage } from './account';

export const metadata = {
  title: 'Account | Homebuddy',
  description: 'Manage your Homebuddy plan, credits, and billing.',
};

export default function Page() {
  return <AccountPage />;
}
